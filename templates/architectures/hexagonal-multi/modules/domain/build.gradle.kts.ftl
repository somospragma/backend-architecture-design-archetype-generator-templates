plugins {
    id("java-library")
}

description = "Domain layer - Pure business logic and rules"

dependencies {
    // No external dependencies - Pure Java domain
    // Only standard Java libraries
}
